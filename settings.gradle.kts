// Root settings — inclut les deux projets Android indépendants.
// Chaque sous-projet (gateway/, caisse-test/) a son propre settings.gradle.kts
// et se compile séparément avec ./gradlew assembleDebug depuis son répertoire.

rootProject.name = "gateway_pos_sdk"
